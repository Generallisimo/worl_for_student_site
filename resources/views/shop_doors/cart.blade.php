                                        <!-- <form action="{{route('shop_update_cart', $cart)}}" method="POST">
                                        @csrf
                                        @method('PUT')
                                            <div class="del-button">
                                                <input type="nubmer" name="quantety" value="{{$cart->quantety}}" min="1" max="10">
                                                <button type="submit"></button>
                                            </div>
                                        </form>     -->